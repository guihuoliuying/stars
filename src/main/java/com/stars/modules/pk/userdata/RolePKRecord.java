package com.stars.modules.pk.userdata;import com.stars.db.DBUtil;import com.stars.db.DbRow;import com.stars.db.SqlUtil;/** * Created by liuyuheng on 2016/11/1. */public class RolePKRecord extends DbRow implements Comparable<RolePKRecord> {    private long roleId;// '角色Id'    private long enemyId;// '对手roleId'    private byte result;// '胜负结果'    private long createTimestamp;// '创建时间戳'    public RolePKRecord() {    }    public RolePKRecord(long roleId, long enemyId, byte result) {        this.roleId = roleId;        this.enemyId = enemyId;        this.result = result;        this.createTimestamp = System.currentTimeMillis();    }    @Override    public String getChangeSql() {        return SqlUtil.getSql(this, DBUtil.DB_USER, "rolepkrecord", " roleid=" + roleId + " and enemyid=" + enemyId);    }    @Override    public String getDeleteSql() {        return SqlUtil.getDeleteSql("roledungeon", " roleid=" + roleId + " and enemyid=" + enemyId);    }    @Override    public int compareTo(RolePKRecord o) {        if (createTimestamp != o.getCreateTimestamp()) {            return createTimestamp < o.getCreateTimestamp() ? -1 : 1;        } else {            return enemyId < o.getRoleId() ? 1 : -1;        }    }    public long getRoleId() {        return roleId;    }    public void setRoleId(long roleId) {        this.roleId = roleId;    }    public long getEnemyId() {        return enemyId;    }    public void setEnemyId(long enemyId) {        this.enemyId = enemyId;    }    public byte getResult() {        return result;    }    public void setResult(byte result) {        this.result = result;    }    public long getCreateTimestamp() {        return createTimestamp;    }    public void setCreateTimestamp(long createTimestamp) {        this.createTimestamp = createTimestamp;    }}