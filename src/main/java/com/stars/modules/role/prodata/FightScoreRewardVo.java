package com.stars.modules.role.prodata;import com.stars.network.server.buffer.NewByteBuffer;import com.stars.util.StringUtil;import java.util.HashMap;import java.util.Map;/** * Created by liuyuheng on 2016/8/1. */public class FightScoreRewardVo {    private int rewardId;// '奖励编号'    private int fightScore;// '战力'    private String rewardItem;// '奖励物品'    /* 内存数据 */    private Map<Integer, Integer> rewardMap = new HashMap<>();    public void writeToBuff(NewByteBuffer buff) {        buff.writeInt(rewardId);        buff.writeInt(fightScore);        buff.writeString(rewardItem);    }    public int getRewardId() {        return rewardId;    }    public void setRewardId(int rewardId) {        this.rewardId = rewardId;    }    public int getFightScore() {        return fightScore;    }    public void setFightScore(int fightScore) {        this.fightScore = fightScore;    }    public String getRewardItem() {        return rewardItem;    }    public void setRewardItem(String rewardItem) throws Exception {        this.rewardItem = rewardItem;        if (StringUtil.isEmpty(rewardItem) || "0".equals(rewardItem)) {            return;        }        rewardItem = rewardItem.replace("+", "=");//        rewardMap = StringUtil.parseIntegerMap(rewardItem, ",", HashMap.class);        rewardMap = StringUtil.toMap(rewardItem, Integer.class, Integer.class, '=', ',');    }    public Map<Integer, Integer> getRewardMap() {        return rewardMap;    }}